**✅ Here's your ready-to-paste REPLIT MASTER PROMPT** (optimized and cleaned for maximum success with Replit AI).

Just create a new **HTML, CSS, JS** Replit (or use the blank template), upload your image as **`portrait.jpg`** in the root (or `/assets/portrait.jpg`), then paste the entire block below into Replit AI and hit **Generate**.

```txt
Build a modern, high-performance website hero section with an interactive pixelated background using p5.js.

PROJECT STRUCTURE:
- index.html
- style.css
- sketch.js
- portrait.jpg (use the uploaded image — face slightly right of center)

GOAL:
Create a fullscreen hero section where a pixelated animated version of the portrait.jpg runs in the background, while keeping the hero text perfectly readable in the foreground. Premium VC-level modern aesthetic, buttery smooth 60fps.

FEATURES:

1. CANVAS BACKGROUND
- Use p5.js (latest version via CDN)
- Render the image as a dynamic pixel grid (each pixel = filled rectangle)
- Base pixel size: 10–14 px
- Canvas must be fully responsive and fullscreen

2. SMART PIXELATION (CRITICAL)
- Pixel size must vary based on distance from focal point
- Focal point = slightly right of center (where the face is)
- Behavior:
  • Near focal point (face area): smaller pixels (high detail, 4–8 px)
  • Outer areas: larger pixels (more abstract, 12–20 px)
- Use distance-based calculation for smooth transition

3. INTERACTIVITY
- Pixels repel from mouse cursor within a radius (repulsion strength ~0.8–1.2)
- Smooth easing back to original position (not instant snap)
- Add subtle continuous floating motion using Perlin noise (very gentle, slow)

4. TEXT-SAFE ZONE (VERY IMPORTANT FOR READABILITY)
- Left 40% of the screen is a protected text zone
- In this zone:
  • Reduce pixel movement and repulsion by 70%
  • Slightly darken pixels and lower contrast
  • Keep pixels smaller to avoid interfering with text

5. READABILITY OVERLAY
- Gradient overlay on top of canvas (z-index layer):
  • Left side: rgba(0,0,0,0.65)
  • Middle fade: rgba(0,0,0,0.3)
  • Right side: transparent
- Overlay must be in CSS so it doesn't affect p5 rendering performance

6. HERO CONTENT (HTML + CSS, positioned above canvas)
- H1: "Guillaume Lauzier"
- Subtitle: "Building the infrastructure of intelligence"
- Style:
  • Clean, minimal, modern sans-serif typography
  • Large white text with subtle text-shadow for extra readability
  • Max width ~620px
  • Vertically centered on left side
  • Responsive (scales nicely on mobile)

7. PERFORMANCE
- Must run at 60fps on laptop/desktop
- Optimize pixel rendering (pre-calculate grid where possible, avoid redrawing unchanged pixels)
- Use requestAnimationFrame properly
- Efficient image pixel sampling (loadPixels() once or smart caching)

8. POLISH
- Smooth transitions, no jitter or lag
- Subtle parallax on mouse move (very light)
- Soft glow on brighter areas of the image (optional but nice)
- Fade-in animation when page loads

OUTPUT:
Give me the complete, ready-to-run code for:
- index.html (with p5.js CDN)
- style.css
- sketch.js

Make sure it works instantly in Replit with zero extra setup or errors. Use modern, clean code.
```

---

### How to use it right now:
1. Go to Replit → New Repl → choose **HTML, CSS, JS**
2. Upload your photo and name it exactly **`portrait.jpg`** (crop it so your face is slightly right of center and left side has breathing room for text)
3. Open the AI chat in Replit and paste the whole block above
4. Hit Generate

If Replit still gives you something weak, reply to it with this one-liner:
> "Refactor this to improve performance and visual smoothness. Use requestAnimationFrame optimization, reduce pixel redraw cost, and make the pixelation more dynamic as described."

Would you like me to also give you the **Advanced version** prompt (with layers, dynamic pixel size on mouse move, cinematic grain, etc.) right now? Or do you want me to wait until you paste what Replit generates and I’ll upgrade it to production level here?

Just say “give me advanced” or “wait for output” and I’ll help you get it perfect. 🚀