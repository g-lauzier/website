# Jekyll build output and caches
_site/
.jekyll-cache/
.jekyll-metadata
.cache/

# Bundler / Ruby
# Anchored to repo root so it only ignores Bundler's vendor dir,
# NOT the app's vendored front-end libs in assets/vendor/.
/vendor/
.bundle/
Gemfile.lock.bak

# Node
node_modules/
npm-debug.log
yarn-error.log

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Logs
*.log

# Local-only / Replit
.local/
attached_assets/
